# 小说续写与长流程审计（2026-09-05）

基线：`c99f8c2` / iter169，开始时 tracked tree 干净。用户授权 WebUI、真实文本模型和长流程测试，累计人民币硬上限 200 元；先报告方案，再起修复迭代，最终必须有真实测试。原文只从用户指定文件复制到全新工作区，起点为第三部结束、第四部前；不复用既有产物，不修改原文，报告不含正文。

## 问题与证据

| ID | 级别 | 问题与触发条件 | 证据 | 修复方案 |
|---|---|---|---|---|
| F01 | P1 | 中文合集全文件同标题去重吞掉不同卷正文，目录与分行标题不匹配，实际章节大量漏切，无法可靠选择续写起点 | 新工作区默认 normalize/split：en_upload，259 项，目录项仅数十字符，最大观察项 216368 字符 | 保留不同位置同名章与尾声；仅过滤可确认的局部目录区块；起点必须基于正文边界验证 |
| F02 | P1 | 正文首次加载前编辑与保存可用；提前聚焦导致加载后仍空 | templates.py:947，static.py:5851/6123，代码审查确认；待延迟浏览器复现 | 只有当前正文 GET 成功才启用；失败保留禁用和重试提示 |
| F03 | P2 | 双标签页旧正文静默覆盖新修改 | 浏览器 A/B 同时加载，A 改首段保存成功，B 改末段保存成功，A 刷新后其首段修改丢失 | GET 返回版本，PUT 锁内比较版本，冲突拒绝并保留输入 |
| F04 | P2 | cancel 与 complete 释放状态锁后追加快照，终态可被旧 running 覆盖 | 纯内存控制时序得到 append_order=[succeeded,running]，重启后会变 lost | 状态转换和落盘保持同一顺序；终态不得被旧快照覆盖 |
| F05 | P2 | 长期重复任务快照使日志超 5000 行或 4 MB，历史读取失效或新任务永久失败 | jobs.py:722/874/2730；读写容量不一致，无压缩 | 安全压缩重复快照，保护 unknown/active/恢复依据，并保持恢复授权对历史变化的检测 |
| F06 | P1 | 缺失 usage 的成功响应按可见文本估算费用，可能低估隐藏 token 后继续计费 | llm_client.py:1013-1023 与预算准入:325-335 | 缺失/无效计费数据时标记不可可靠结算并阻断新增请求；不得当零费用 |
| F07 | P1 | force 重写前章没有失效后续已审核正文和实体推进；恢复可能跳过旧后章 | runner:648，story_memory 仅 Web 保存失效；代码证据，待执行回归 | 实际重写前持久失效后续，生成前清理旧衍生上下文，失败也不能恢复旧完成态 |
| F08 | P2 | legacy write --force 向前章注入未来摘要与结尾 | 源函数内存复现：重写1章得到 END_FIVE，future chapter present=True | writer 实际生成前做目标章 cutoff，兼容正常 skip |
| F09 | P2 | 上手指南 write_book.sh --tier mid 入口直接失败 | 实测 exit64，Unknown arg: --tier；现有聚焦测试仍通过 | 支持 tier 参数解析/透传，补真实 argv 合同测试 |

额外候选：rolling JSON 的 chapters 列表含非对象时 AttributeError；尚无正常写入可产生该状态的证据，作为恢复防御项，不与已确认用户流程问题混称。

## 已执行与边界

- 三路独立只读审计：runner/correctness、WebUI/并发、计费/真实入口。
- 17 项聚焦 mock 测试通过；尚未执行 canonical 全量。
- 本地隔离 Web 服务与实际浏览器双标签保存复现；测试使用合成内容。
- 用户指定原文 SHA256：`1db6bd8d4417061b8c2507b950130fa015a433aab2af34579b1584afe3d66b54`，6731543 bytes。只在干净工作区进行原文导入，不输出正文。
- 截至本报告初稿：真实请求 0，真实费用 0 元；尚不能声称 provider-validated 或长跑通过。

## 修复与验收计划

起 iter170，先修导入起点、编辑保护、计费缺失保护，再闭合重写记忆与任务持久化，补入口参数。聚焦回归后进行 correctness、security/boundary、Web/计费专项独立审查。使用新工作区、指定起点进行真实准备→规划→续写→多章/恢复测试，累计最坏请求费用受外层 200 元预算约束，未知提交停止并保留证据。最终 implementation commit 上运行一次 canonical 验证。mock 通过不替代用户要求的真实验收；任何未通过项明确保留为未完成。

复核更正：en_upload 是 ASCII 文件名的命名规则，实际语言检测为中文，不是英文误判。根因是全文件标题去重与目录过滤。

## iter170 修复阶段证据

F01–F09 已实现针对性修复并通过聚焦与三路增量审查。真实浏览器验证了加载门禁及双标签冲突；真实原文导入保留88章，起点为第三部尾声，归一化正文止于139405行，源文件哈希未变。代码审查不意味着不存在其他设计缺陷。

真实请求1次，模型openai/gpt-5.5-low，上游RateLimitError，约1.27秒，准备流程首章即停止。最坏费用预留¥2.79036，未确认实际账单；不重提历史unknown。最终真实单章/长跑尚未通过，不能将本轮写作验收标成完成。canonical证据另由iteration记录。

额外防御候选（rolling合法JSON但非对象条目）本轮未修：没有正常内部写入反例，不与已确认流程阻塞混称闭合。

## F10：阶段预算固定导致无法从额度不足恢复

真实浏览器发现准备固定3元，代码确认大纲8元、细纲2元亦无输入；正常API分别支持续写准备20元、大纲33元、细纲10元。用户不能在页面调整，反复重试会重复相同限额。方案：三个阶段增加预算输入，保持默认3/8/2；准备按原创/续写限制3/20；确认弹窗沿用实际参数。正文及恢复独立6元UI边界本轮不扩展。

恢复测试：用户确认服务恢复并明确授权向当前域名发送指定内容后，WebUI真实准备请求17.20秒返回BadGatewayError；新增不含原文的最小连通性请求亦失败。累计3次请求保守预留5.609916元，实际费用未知；暂停后续真实调用。

F10已修复：139项聚焦、两路审查及实际浏览器预算确认/超限/必填校验通过。实现087aaba完整canonical 1928项通过，仅mock-functional；真实验收仍未通过。

## F11：取消检查未覆盖同章模型修复/分块

真实取消已验证在途成功章保留、下一章不发送；独立代码审查确认progress检查之间的JSON repair与chunk仍可能在取消后新增付费请求。方案：使用上下文隔离的每请求取消回调；准入前检查并以terminal异常原样传播，避免被模型故障或修复链吞掉。成功在途响应继续保留，不承诺强行中止远端请求。

## F12：当前服务未执行输出token上限（上游限制，用户已要求继续）

2026-09-06 新key与openai/gpt-5.6-luna：最小生成HTTP200、约1.95秒、15token。随后直接请求max_tokens=16，实际completion_tokens=203；改为max_completion_tokens=16，实际214；两次均输出完整1–100数字列表，finish_reason=stop。两种参数都未生效，不能依靠客户端输出上限证明每请求费用上界，当前暂定单价来自用户USD0.01/百万输入输出token，不是独立账单证据。已请求停止真实准备，保留成功文件；完整provider验收仍未完成。应由服务修复长度约束，或提供可靠服务端费用上限后继续。

附加UX待办：准备任务运行时概览显示“等待中”，与工作台不一致；长章三块提取只有章级进度，缺分块反馈。只读确认前者忽略recent_job，后者未显示chunk，未纳入F01–F11已修清单。

2026-09-06先前暂停快照（后续已恢复）：7章提取JSON落盘，任务明确aborted/cancelled；累计16次请求（旧模型3、新模型13，含3个诊断请求），Luna按用户单价估算含诊断约0.019253元，非服务端账单。含此前未知费用的账本预留5.667020352元；发现输出限制不生效后不把该预留宣称严格上界。专用含key进程已退出，后续无后台模型调用。真实准备/规划/正文全链仍未验收。

## F13：概览忽略正在执行的准备任务

真实rebuild运行时概览显示等待中和下一步整理设定，与工作台正在处理不一致。方案：使用后端已提供且优先active的recent_job，运行时显示任务并轮询，终态刷新已保存结果；无任务时用准备阶段描述产物状态，状态读取失败/lost显示待确认。仅修概览，不混入分块进度待办。用户已明确暂不考虑预算、继续测试，F12保留问题但不再作为本轮暂停条件。

## F14：首次细纲未验证请求章节覆盖

只读审查发现 `generate_chapter_plan` 的 fresh 分支直接保存模型返回；schema 允许空章节列表，也不保证章节数量和编号符合用户请求。请求三章而模型返回两章时会误报生成成功，写作在缺失计划处才阻断。方案：首次生成与 append 一样先检查足量，再按请求数量截取并规范为1..N，将 target_chapters 设为实际请求数量；不足时拒绝落盘，force 场景保留原文件，不新增自动模型重试。添加空/不足/超量/重复编号及旧文件保护回归，再独立审查。

F14增量验收：新增测试最初误归无隔离类，独立审查发现后移入临时workspace fixture，47项聚焦及两路复审通过；新建合成目录移出仓库。实现8bf8b6d canonical1934项/15步骤/87秒通过。

## F15：Web 强制非流式与长响应网关不兼容

真实compress两次非流式约125秒Timeout；相同摘要独立CLI流式155.43秒成功。配置request_timeout=400，依赖路径无120秒本地封顶；推断上游长时间无数据限制，但不外推为已确认网关实现。Web因同步next无法强制截止而禁用流式，直接删除保护会恢复长跑失控。方案：新增显式OPENAI_WEB_STREAM配置，仅openai/兼容入口使用原生异步SDK流，在请求前原线程计数/预留；连接和每次读取受总deadline及取消检查约束，完成后主线程记账。原生usage不经LiteLLM估算补全；缺usage沿用降级阻断。断流、无终态、超大输出不返回partial、不自动重发；默认路径不变。真实重建及后续阶段使用该选项复验。

F15审查补强：原生SDK保留GPT-5/o参数映射；spawn进程隔离DNS与清理，父进程非阻塞有界JSON分帧接收，半帧卡死亦能终止。取消/断流记录submission_unknown、usage未知与预留，费用累计不释放预留且终态不重复。仅POSIX/openai/显式开启；子进程加载模块但不执行业务步骤或写业务产物/账本。70项流式/LLM聚焦和32项费用配置聚焦通过。

## F16：工作台顶栏与任务卡片状态不同步

真实Web准备succeeded/100%卡片仍可取消；本页启动debate后任务卡片running而顶栏长期pending/等待中。根因：pollJob终态前无条件渲染取消按钮；本页启动的poll只刷新任务卡片，顶栏进度仅刷新恢复任务的另一轮询更新。方案：终态不显示取消或取消等待；本页与恢复路径共用按job_id绑定的顶栏更新，错误/未知状态保持锁定，无第二条轮询或额外模型请求。使用实际JS状态转换回归并做独立审查。

F16增量验收：补齐所有pollJob调用路径；审查发现并修复busy清理解除未知锁及恢复轮询旧响应覆盖。44项聚焦、两路复审通过。隔离真实浏览器确认运行20%顶栏与卡片同步、failed无取消、断网后生成设定仍disabled。

## F17：大纲投票恢复丢失且问题未绑定

只读复核确认：恢复时把旧投票记入done_ballots，却在重写debate_log时删除这些行，后续又跳过重投，导致裁决静默缺票。另每次恢复重建决策问题，旧票仅按question_index绑定，不能安全复用到新问题。方案：在投票前原子保存决策检查点，绑定完整发言摘要与agent名单；恢复同时保留检查点和成功投票，复用相同问题。旧日志已有票但无检查点时保留原文件并明确阻断，不能偷偷删除或把旧票用到新问题。增加部分投票中断、再恢复、旧日志保护反例。

## F18：未知模型失败误称响应超时

真实流式请求22.7秒失败只能证明未取得完整响应，不能证明超时。当前submission_unknown通用标题一律写超时。最小修复改为“模型请求未完成，结果待确认”，保持未知计费预留、零自动重试及人工核对语义。细分传输诊断另列后续范围，不输出上游异常原文。

## F19：多章写作仍被单章请求上限截断

工作台与批量续写允许多章，但write-book服务端默认和最大均20次；标准单章正常路径约15次，三章至少约45次，用户无法调高，必然提前停止。方案：普通write-book默认和最大按20次/章计算、最多160次；显式输入仍尊重用户选择，全局硬上限160次，预算/时限/取消门禁保持。两处UI默认随章节数更新，仅未手改请求上限时联动；单章失败恢复仍20次，不扩大恢复授权。增加默认缩放、超限拒绝、手改保留与确认参数回归。

## F20：长辩论到裁决阶段必然上下文溢出

真实36条发言完整保存后，裁决在本地context_too_large门禁失败，未发新请求。根因摘要只按条数裁剪，每条发言无长度界；其余消费者再整串截头，后轮/后角色被丢。方案：逐条头尾摘录、明确省略、覆盖全部轮次与角色、序列化后验证长度，按消费者限额构造完整JSON；裁决按冻结client上下文预算有限收缩摘要，固定内容超限仍明确阻断。大纲保留完整裁决问题，不裁剪问题列表。只改变发送的派生上下文，完整发言日志和checkpoint指纹不变，不新增压缩模型请求。

## 保留的低优先级体验改进

- P3：工作台的细纲/正文确认框只显示“当前阶段”和费用、请求、时限，不重复显示目标章节数。表单及冻结job参数正确（本次target_chapters=3、chapters=3），建议确认框直接展示章节范围，减少离开表单后的核对负担。
- P3：单章评审阶段进度只按阶段更新，多位评审逐一返回时页面百分比保持不变。真实响应可间隔数十秒，建议展示“已完成N/M位评审”或最近活动时间；保持不输出模型原文或内部提示。

## F21：内审中断后完整正文不能复用

真实第一章正文已完整返回，内审流式失败后保存19571字节partial及hash，但失败记录缺来源上下文，普通resume把它当缺章重新写；Web审核仅接受canonical正文。方案：失败件增加版本、当前run_context、完整正文阶段标记；用户新提交normalresume且canonical不存在时，只在hash/context/stage匹配后跳过首轮写作，重新lint和完整内审。不复用零散评审，不将partial直接改名成批准正文；force和内部retry不复用。旧版/不匹配失败件先保留快照，再正常生成，快照失败不发模型请求。此轮不提供SIGKILL时内存稿恢复，未知调用仍终止原任务。

F21真实增量（2026-09-06）：45a2bf7 canonical 1971项/15步骤/102秒通过。577ecfea任务正文108.3秒返回5753token后，在审查期间从浏览器取消；schema v2、stage=review、resume_from_stage=lint，19515字节正文与记录hash一致。7244078f恢复任务直接进入review，仅新增一个审查请求，未重复生成正文；全审查与三章链继续执行。旧版19571字节失败稿已按原hash保存独立快照。

F22（P2）：F19仍将默认每章20次当作可申请最大值，标准档一次内部重写即可能需21–22次，单章用户不能提高。方案：默认仍按章数×20、最多160；write-book显式最大值独立为全局160，其他步骤不变。现有真实任务尚未触发此耗尽，不冒充已复现耗尽；不修改运行中额度。

F23（P2）：写手收到本章计划必做事件，模型评审/顾问/简化回退不含计划（只有投票后确定性履约检查）。真实多轮Reject提示与新增线索相关，不能认定所有拒稿误判，但输入不对称已代码确认。方案：仅注入当前已传入计划的指纹覆盖白名单、总6000字符以内的摘要，统一区分待发生计划与既有硬事实；不新增读取未来章/大纲，不降低阈值。修复后同一完整失败稿恢复真实审查。

F23真实增量：b2e88fa canonical 1975项/15步骤/96秒通过。a69f2a29从19337字节检查点恢复，零正文重写进入新评审；首章内审5票/7.56分通过，外审及strict最终也通过，正文SHA保持1cd58c2b…，rolling绑定一致。任务自动进入第二章。评分门槛仍mid 4票且7.5分，不将局部通过扩大为三章完成。

体验待办：真实第二章曾内审5票/7.64通过、外审同稿5票/7.46拒绝；后续修订也出现5票/7.34拒绝。当前合同同时要求票数与分数，行为符合代码，不能直接判定误拒；但临界评分波动会触发整章外层重写，需独立校准评审稳定性与成本。此轮不降低门槛、不把赞成票替代严格通过。另工作台曾状态读取失败锁定，取消由已有HTTP接口完成，未复现根因，不宣称已修复。

F24（P1）：真实a69f2a29在第三章外审4/6响应时，require_external_review仍返回approved，因为内外审共用同名报告且无来源完成凭证；45分钟截止于顾问请求，下一次resume可能直接SKIP。方案：仅review_target完整返回后写外审完成标记，严格状态与meta同步要求标记；主审通过但外审缺标记走补审，不重写，旧报告保守补审。不能把此时三章approved投影作为完整真实验收。
